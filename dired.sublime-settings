{
  // If you don't like `..` symbol on the top of each view that moves you to
  // the parent directory and want to hide it you can set this to false
  "dired_show_parent": true,

  // If you want to see header (underlined full path) on top of file list
  "dired_header": true,

  // If you want to see full path in tab title and thus in window title
  // if tab is focused
  "dired_show_full_path": false,

  // If true a dialog box asks for confirmation for any delete operations
  "dired_confirm_deletions": true,

  // Not showing hidden files by default
  "dired_show_hidden_files": false,

  // Not showing system files by default (Windows only)
  "dired_show_system_files": false,

  // Add patterns used to determine if a file should be hidden
  // e.g. [".*", "__pycache__", "*.pyc"]
  // Note that we already honor Sublime's standard 'folder_exclude_patterns'
  // setting, that's why this *additional* dired setting is kept empty
  // by default.
  // Also note, that, on Windows, we always read out the actual file-system
  // hidden attribute.
  "dired_hidden_files_patterns": [],

  // Show full file stats (size, last modified date) by default
  "dired_show_stats": true,

  // Which path separator to display for directories in the view:
  // - "native": use os.sep for the current platform (default)
  // - "posix": always display "/" (useful for debugging)
  // - "windows": always display "\\"
  "dired_display_path_separator": "native",

  // false: hijacking is disabled
  // "jump_list": shows the project jump list
  // "dired"    : shows a normal FileBrower view defaults to user directory
  "dired_hijack_new_window": false,

  // Path to git executable.
  // Wildcards are supported, e.g. if you use GitHub for Windows it would be:
  //     "git_path": "~/Appdata/Local/GitHub/PortableGit_*/cmd/git.exe"
  // To disable git integration set it to false:
  //     "git_path": false
  "git_path": "git",

  // Enable hg support, typically:
  //     "hg_path": "hg",
  // Can be also a path to an hg executable, wildcards are supported,
  // e.g. if you use TortoiseHg it would be:
  //     "hg_path": "%ProgramFiles%/TortoiseHg/hg.exe"
  "hg_path": false,

  // true: the default, pressing Enter on a directory uses the current view.
  // false: a new view is created.
  "dired_reuse_view": true,

  // How should be positioned the Browse Mode view opened when jumping
  // "left": the default, open the view on the left
  // "right": open the view on the right
  // true: take all space available
  // false: don’t open any view
  "dired_open_on_jump": "left",

  // true: keep the active window if empty when jumping
  // false: the default, always open a new window when jumping
  "dired_smart_jump": false,

  // Enable Sublime's stock history feature ("jump_back/jump_forward") for
  // dired views.  (Technically, this controls if we set "is_widget" on our
  // views.)
  "dired_enable_stock_sublime_history": false,

  // Width for FileBrowser column (as sidebar). Set to two floats for a
  // dynamic width (the default), or to a single float or int, i.e.
  // float  0.1-0.9   put 1 is a window width, so e.g. 0.5 is half of window
  // int    e.g. 99   pixels, if value will be more than current window width
  //                  then value will be treated as 0.9
  "dired_width": [0.25, 0.5],

  // Automatically refresh view(s) in case of any changes in open directories
  "dired_autorefresh": true,

  // String to place between file name and generic number in case of conflicting
  // filenames (i.e. duplicate, copy, move), e.g.
  //   file.ext → file — 2.ext
  // Only for OSX and Linux.
  "dired_dup_separator": " — ",

  // Automatically disable Vintageous to avoid keybindings incompatibilities;
  // note it is Vintageous setting, if it does not work you should report into
  // appropriate repository https://github.com/guillermooo/Vintageous/issues
  "__vi_external_disable": true,

  // The rest of these settings are normal sublime settings to
  // make FileBrowser views look more like a file browser than an editor
  // This plugin does not change font and font size, but it's
  // highly recommended to customize them in your user files.
  // We ship a thTheme actually and you could turn it on here.
  // However, your normal color-scheme should look okay and should fit
  // your design.  Just note that you might want to add some scopes to
  // it.  Namely: `dired.marked`, `dired.copied`, and `dired.cut` are
  // use as highlightings and for sure not defined by default in your scheme.
  // Also the vcs markers `item.modified.dired` and `item.untracked.dired`.
  // "color_scheme": "Packages/FileBrowser/dired.hidden-tmTheme",
  "line_numbers": false,
  "word_wrap": false,
  "spell_check": false,
  "scroll_past_end": false,
  "draw_indent_guides": false,
  "fold_buttons": false,
  "drag_text": false,
  "highlight_line": true,
  "margin": 0,
  "tab_size": 3,
  "rulers": [],
  "draw_unicode_white_space": "none",
  "caret_extra_width": 0 // extra caret width causes partially hidden chars
  // "font_face": "comic sans",
  // "font_size": 13,
}
